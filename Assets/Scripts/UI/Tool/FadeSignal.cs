/// <summary>
/// 페이드를 일으키는 신호 목록. 인스펙터에서 드롭다운으로 고른다.
/// 신호를 늘리고 싶으면 여기 한 줄 추가하면 끝 — 그 즉시 모든 Fader/FadeSender의 드롭다운에 나타난다.
/// (쉐이크 신호와 절대 안 섞인다. 페이드 드롭다운에 "플레이어피격"이 뜨면 그게 억지로 묶은 것이다.)
/// </summary>
public enum FadeSignal
{
    없음 = 0, // 신호로 안 움직임. 버튼 드래그나 코드로만 부를 때.
    방이동 = 1,

    // ★ 신호를 추가할 땐 반드시 '맨 아래에 새 번호로' 추가할 것.
    //    인스펙터엔 이름이 아니라 번호가 저장되기 때문에, 중간에 끼워넣으면 아래 것들이 전부 밀려서
    //    이미 세팅해둔 컴포넌트들이 조용히 딴 신호를 듣게 된다. 번호는 한 번 정하면 절대 안 바꾼다.
    // 보스등장 = 2,
}
